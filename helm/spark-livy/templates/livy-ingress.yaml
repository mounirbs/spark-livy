apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: livy
  namespace: {{ .Values.metadata.namespace }}
spec:
  rules:
    - http:
        paths:
          - backend:
              service:
                name: livy
                port:
                  number: {{ .Values.apacheLivy.livyPort }}
            path: /
            pathType: Prefix
